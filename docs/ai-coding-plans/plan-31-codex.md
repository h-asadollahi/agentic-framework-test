# Codex Plan 31

1. Extend demo markdown renderer to parse GitHub-style markdown tables (`| ... |` + separator row) into semantic HTML tables.
2. Preserve existing heading/list/paragraph behavior while ensuring tables are detected before paragraph fallback.
3. Add table-specific styling in `demo/styles.css` for readability and horizontal overflow handling.
4. Verify with a live prompt that currently renders raw table markdown.
5. Update handover, commit, and push.
